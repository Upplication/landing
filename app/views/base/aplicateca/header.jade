header.padding-top
    a.logo.margin-bottom(title='#{header.logo.title}')
        img(src="#{localConfig.base_path}/images/upplication.png", alt="#{header.logo.title}", title="#{header.logo.title}")

    a.icon-list
        span Menu
    nav.main-menu.uppercase.unstyled

        ul#sections
            include header-links

    a.login.unstyled(href="#{$.aplicateca.header.url}", title="#{aplicateca.header.title}") #{$.aplicateca.header.anchor}